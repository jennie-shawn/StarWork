# M2.11 Knowledge Capability SPEC

## 背景

StarWork 早期结构里曾出现 `知识/` 或 `knowledge/`，但它们的产品边界不够清楚：有时像资料夹，有时像共享知识入口，有时又像项目自己的长期记忆。

本轮需求要把知识库重新定义清楚：

> 参考资料是输入，输出是成果，知识库是 Agent 持续维护的长期理解层。

知识库不是基础目录，也不是普通资料堆放区。它是一种可选能力：用户需要长期沉淀理解时，再为项目工作台开启。

## 目标

1. 把知识库定义为可插拔能力，而不是 Project Kit 默认目录。
2. 明确知识库和 `参考资料/`、`输出/`、项目中心共享知识的边界。
3. 明确知识库目录结构，尤其是 `schema.md`、`pages/`、`synthesis/` 的作用和产生机制。
4. 明确 CLI 和 Skill 如何配合创建、检查、整理知识库。
5. 规划 `starworkKnowledge` skill 的一期能力。
6. 规划 CLI、doctor、upgrade、init 和文档需要配合调整的地方。

## 不做什么

本轮暂不做这些事：

- 不做“把项目知识提交到项目中心共享知识库”的流程。
- 不做项目中心共享知识库的审核、合并和分发机制。
- 不强制迁移用户已有的 `知识/` 或 `knowledge/`。
- 不把知识库能力默认装进所有项目。
- 不把知识库当成 Pack 的普通业务目录。

## 核心产品判断

### 判断 1：知识库是能力，不是基础骨架

Project Kit 只应该提供项目工作台的最小骨架，例如 Agent 入口、系统状态、运行配置和 skill 挂载位置。

知识库是否存在，取决于用户是否需要长期积累和复用理解。因此：

```text
项目工作台
+ 知识库能力
= 带长期知识沉淀的项目工作台
```

而不是：

```text
Project Kit 默认包含 知识/
```

### 判断 2：知识库不是参考资料

`参考资料/` 是用户提供的原始材料，例如 PDF、网页、访谈记录、截图、会议纪要、案例库。

知识库是 Agent 整理后的长期理解，例如概念、主题、方法、关系、判断、阶段性总结。

简单区别：

| 目录 | 作用 | 是否需要整理 |
|---|---|---|
| `参考资料/` | 原始输入 | 不一定 |
| `输出/` | 当前项目成果 | 按任务交付整理 |
| `知识库/` | 长期可复用理解 | 必须由 Agent 持续整理 |

### 判断 3：知识库不是项目中心共享知识

项目知识库服务当前项目。

项目中心共享知识服务多个项目。

本轮只设计“项目知识库”。项目知识是否能进入项目中心共享知识，后续另开需求设计。

### 判断 4：知识库要有维护机制

如果只是建一个目录让 Agent 随便写，它很快会变成新的资料堆。

所以知识库必须同时定义：

- 什么内容值得进入知识库。
- 新资料如何吸收。
- 主题页如何更新。
- 综合判断如何形成。
- 索引和维护日志如何更新。
- 哪些内容不能进入知识库。

## 用户侧表达

对用户不要说复杂术语。

推荐表达：

- “开启项目知识库”
- “整理项目知识库”
- “让 AI 把长期有用的内容沉淀下来”
- “检查知识库是否混乱”

避免表达：

- “启用 knowledge capability”
- “执行 ingest / synthesis pipeline”
- “更新知识 schema”

内部文档和代码可以使用 `knowledge capability`，但用户文案应尽量说人话。

## 标准目录结构

中文项目工作台开启知识库后：

```text
知识库/
├── README.md
├── index.md
├── schema.md
├── log.md
├── inbox/
├── sources/
├── pages/
└── synthesis/
```

英文项目工作台开启知识库后：

```text
knowledge-base/
├── README.md
├── index.md
├── schema.md
├── log.md
├── inbox/
├── sources/
├── pages/
└── synthesis/
```

### 为什么不用 `知识/` 和 `knowledge/`

`知识/`、`knowledge/` 在旧结构中含义过多：

- 可能是项目本地知识。
- 可能是项目中心共享知识。
- 可能是历史模板留下的资料目录。
- 可能是只读链接。

新能力建议使用更明确的路径：

| 语言 | 新路径 | 含义 |
|---|---|---|
| 中文 | `知识库/` | 当前项目自己的长期知识库 |
| 英文 | `knowledge-base/` | Current project knowledge base |

旧路径由 `doctor` 和 `upgrade` 识别，但不作为新项目标准路径。

## 文件和文件夹职责

### `README.md`

给人类和 Agent 的入口说明。

它应该说明：

- 这个目录是项目知识库，不是原始资料区。
- 原始资料应放在 Pack 定义的参考资料区。
- 重要知识应进入 `pages/` 或 `synthesis/`。
- 更新知识库前应先读 `schema.md` 和 `index.md`。

### `index.md`

知识地图。

它负责告诉 Agent：

- 当前知识库有哪些主题。
- 哪些页面最重要。
- 哪些综合判断已经形成。
- 相关页面之间大概如何连接。

`index.md` 不应变成所有内容的正文集合。它是导航，不是知识正文。

### `schema.md`

知识库的整理说明书。

它不是放知识的地方，而是告诉 Agent 应该怎么维护这个知识库。

必须包含：

- 什么内容值得进入知识库。
- 什么内容只应留在参考资料或输出中。
- `pages/` 的写作规则。
- `synthesis/` 的写作规则。
- 命名规则。
- 引用来源规则。
- 更新 `index.md` 和 `log.md` 的规则。
- 不要生成空泛总结、不要堆砌未验证判断。

一句话：

> `schema.md` 管“怎么整理知识”。

### `log.md`

维护记录。

它记录知识库发生过哪些重要变化，例如：

- 新增了哪些主题页。
- 合并了哪些重复主题。
- 根据哪批资料更新了哪些页面。
- 形成了哪些综合判断。
- 哪些内容仍待整理。

`log.md` 不是工作日志，不记录普通任务过程，只记录知识库维护动作。

### `inbox/`

待整理入口。

适合放：

- 用户要求 Agent 后续整理的知识候选。
- 从参考资料里摘出的待判断片段。
- 尚未确定该进入哪个主题页的内容。

`inbox/` 不能长期堆积。skill 应提醒 Agent 定期清理。

### `sources/`

来源索引。

它不复制所有原始资料，而是记录知识库引用过哪些来源。

可以包含：

- 来源文件清单。
- 来源摘要。
- 关键出处链接。
- 原始资料在 `参考资料/` 中的位置。

### `pages/`

稳定主题页。

它记录一个个可长期更新的主题、概念、对象或方法。

例子：

```text
pages/
├── 用户画像.md
├── 标题结构.md
├── 发布节奏.md
├── 账号定位.md
└── 竞品账号.md
```

它回答：

- 这个东西是什么？
- 目前我们知道什么？
- 有哪些规律、例子和反例？
- 和哪些主题有关？
- 这些知识来自哪里？

一句话：

> `pages/` 管“一个个长期主题”。

### `synthesis/`

综合判断和阶段性理解。

它把多个主题页、来源和项目经验组合起来，形成更高一层的判断。

例子：

```text
synthesis/
├── 30天账号冷启动策略.md
├── 内容增长方法论.md
├── 选题到发布工作流.md
└── 账号定位调整建议.md
```

它回答：

- 基于已有知识，现在应该怎么理解？
- 多个主题之间有什么关系？
- 当前阶段应该怎么做？
- 最近一段时间有什么趋势？
- 哪些知识可以组成一套方法？

一句话：

> `synthesis/` 管“基于多个主题形成的判断和洞察”。

## `pages/` 和 `synthesis/` 的区别

最简单的判断：

```text
pages/      = 知识卡片
synthesis/  = 思考成果
```

更具体一点：

| 对比项 | `pages/` | `synthesis/` |
|---|---|---|
| 关注对象 | 单个主题、概念、对象、方法 | 多个主题组合后的判断 |
| 典型标题 | 用户画像、标题结构、竞品账号 | 冷启动策略、增长复盘、定位调整建议 |
| 主要来源 | 新资料、新观察、新经验 | 用户问题、策略判断、阶段复盘 |
| 更新频率 | 较高 | 较低 |
| 作用 | 让知识库记得住 | 让知识库想得明白 |

## 产生机制

### `pages/`：资料触发

`pages/` 通常由新资料、新观察、新经验触发。

```text
新资料 / 新对话 / 新复盘
        ↓
Agent 判断是否有长期有用的主题
        ↓
如果是新主题：创建 page
如果是旧主题：更新已有 page
        ↓
更新 index.md 和 log.md
```

例子：

用户给了一批小红书爆款案例。Agent 发现里面反复出现“标题结构”这个主题。

- 如果已有 `pages/标题结构.md`，就补充进去。
- 如果没有，就新建 `pages/标题结构.md`。

`pages/` 不是为了完成一次交付而写，而是为了长期积累主题知识。

### `synthesis/`：问题触发

`synthesis/` 通常由复杂问题、策略判断、阶段复盘触发。

```text
用户提出复杂问题
        ↓
Agent 读取 index.md 和相关 pages
        ↓
整合、比较、判断
        ↓
生成或更新 synthesis
        ↓
必要时反向更新 pages
        ↓
更新 index.md 和 log.md
```

例子：

用户问：

```text
我这个账号接下来 30 天应该怎么做？
```

Agent 可能读取：

```text
pages/用户画像.md
pages/账号定位.md
pages/标题结构.md
pages/发布节奏.md
pages/竞品账号.md
```

然后生成：

```text
synthesis/30天账号冷启动策略.md
```

`synthesis/` 不是资料一来就生成，而是在需要形成判断时生成。

## 不应进入知识库的内容

以下内容默认不进入知识库：

- 原始 PDF、截图、网页全文。
- 临时草稿。
- 用户还没有确认的正式成果。
- 单次任务过程。
- 没有长期复用价值的聊天记录。
- 没有来源、没有上下文的空泛总结。

这些内容应留在 Pack 定义的参考资料区、草稿区、成果区或事项工作区。

## CLI 和 Skill 的分工

知识库能力不能只靠 CLI，也不能只靠 Skill。

CLI 适合做稳定、可重复、可测试的动作。Skill 适合做人类访谈、理解项目、判断内容该怎么整理。

| 层级 | 负责什么 | 不负责什么 |
|---|---|---|
| CLI | 创建标准目录、写入模板、更新配置、输出结构诊断、按 blueprint 执行变更。 | 判断哪些内容值得沉淀、总结资料、生成主题页和综合判断。 |
| `starworkKnowledge` Skill | 采访用户、理解项目、判断知识库是否需要开启、生成或更新知识页、维护索引和日志。 | 绕过 CLI 随意创建结构、把原始资料整包搬进知识库、自动提交项目中心。 |

核心原则：

```text
CLI 负责“把骨架搭对”
Skill 负责“把知识整理对”
```

## 创建知识库的用户流程

### 流程 A：用户只想快速开启

用户表达：

```text
给这个项目开一个知识库
```

推荐执行：

1. Agent 使用 `starworkKnowledge` skill。
2. Skill 判断当前目录是否是 StarWork 项目工作台。
3. Skill 询问用户：是否现在开启项目知识库。
4. 用户确认后，Skill 调用 CLI：

```bash
starwork knowledge init
```

5. CLI 创建标准结构。
6. Skill 根据项目情况补充 `schema.md`、`index.md` 和 AGENTS 规则。

### 流程 B：用户正在 init 新项目

`starwork init` 默认不创建知识库。

但 init 的友好交互中可以增加一个低优先级问题：

```text
你是否希望这个项目长期沉淀知识库？
```

推荐选项：

```text
暂时不需要（推荐）
需要，帮我开启项目知识库
```

如果用户选择需要：

1. `init` 先完成项目工作台和 Pack 创建。
2. 再调用同一套知识库创建能力。
3. 最终结果与单独运行 `starwork knowledge init` 保持一致。

### 流程 C：用户有旧资料或旧 `知识/knowledge`

用户表达：

```text
我这个项目以前有一些知识材料，帮我整理成 StarWork 知识库
```

推荐执行：

1. Agent 使用 `starworkDoctor` 或 `starworkKnowledge`。
2. CLI 输出结构和文件信号。
3. Skill 判断旧目录更像参考资料、知识草稿、旧知识库，还是共享链接。
4. Skill 生成知识库整理 blueprint。
5. 用户确认。
6. CLI 只负责创建结构和执行安全的文件操作。
7. Skill 负责提炼、合并、更新 `pages/` 和 `synthesis/`。

## `starwork knowledge` 命令组

建议新增一个明确的命令组：

```bash
starwork knowledge <command>
```

一期命令建议如下。

### `starwork knowledge init`

创建项目本地知识库标准结构。

示例：

```bash
starwork knowledge init
starwork knowledge init --language zh
starwork knowledge init --path 知识库
starwork knowledge init --dry-run
```

职责：

- 判断当前工作台语言。
- 创建 `知识库/` 或 `knowledge-base/`。
- 写入 `README.md`、`index.md`、`schema.md`、`log.md`。
- 创建 `inbox/`、`sources/`、`pages/`、`synthesis/`。
- 更新 `.starwork/workspace.json`，声明已开启知识库能力。
- 可选更新 AGENTS 规则中的知识库读取说明。
- 支持 `--dry-run`。
- 如果目录已存在，不覆盖用户内容，只报告已有状态。

不做：

- 不读取和总结用户资料。
- 不自动创建主题页。
- 不迁移旧 `知识/knowledge`。
- 不连接项目中心共享知识库。

### `starwork knowledge status --json`

输出当前项目知识库状态，供 Skill 判断。

示例：

```bash
starwork knowledge status --json
```

建议输出：

```json
{
  "ok": true,
  "enabled": true,
  "language": "zh",
  "root": "知识库",
  "structure": {
    "readme": true,
    "index": true,
    "schema": true,
    "log": true,
    "inbox": true,
    "sources": true,
    "pages": true,
    "synthesis": true
  },
  "counts": {
    "pages": 4,
    "synthesis": 1,
    "inbox": 2,
    "sources": 6
  },
  "legacy_candidates": ["知识"],
  "warnings": []
}
```

注意：

- 输出只描述事实，不给“下一步建议”。
- 避免影响 Skill 的独立判断。

### `starwork knowledge check`

只检查结构完整性，不判断内容质量。

示例：

```bash
starwork knowledge check
starwork knowledge check --json
```

检查项：

- 标准文件是否存在。
- 标准目录是否存在。
- `index.md` 是否为空。
- `schema.md` 是否为空。
- `log.md` 是否存在。
- `pages/` 和 `synthesis/` 是否可读写。

内容是否空泛、主题是否重复、判断是否可靠，由 Skill 做。

### `starwork knowledge apply --blueprint <file>`

按 blueprint 执行结构性变更。

示例：

```bash
starwork knowledge apply --blueprint knowledge-blueprint.json
starwork knowledge apply --blueprint knowledge-blueprint.json --dry-run
```

适用：

- 从旧 `知识/` 整理出新 `知识库/`。
- 创建标准结构。
- 安全移动或复制经过用户确认的文件。
- 更新 AGENTS 规则。

不适用：

- 生成知识正文。
- 总结资料。
- 判断内容价值。

这些仍由 Skill 完成。

## Knowledge Blueprint

当涉及旧目录整理、用户定制路径或 AGENTS 更新时，Skill 应先生成 blueprint，再由 CLI 执行。

建议格式：

```json
{
  "version": "0.1",
  "type": "starwork.knowledge",
  "language": "zh",
  "root": "知识库",
  "actions": [
    {
      "type": "create_knowledge_base",
      "path": "知识库"
    },
    {
      "type": "create_dir",
      "path": "知识库/pages"
    },
    {
      "type": "write_template",
      "path": "知识库/schema.md",
      "template": "knowledge.schema.zh"
    },
    {
      "type": "append_agents_rule",
      "path": "AGENTS.md",
      "section": "知识库"
    }
  ],
  "preserve": [
    "知识/",
    "参考资料/"
  ],
  "notes": [
    "旧 知识/ 暂不迁移，等待用户确认其真实用途。"
  ]
}
```

Blueprint 约束：

- 必须声明语言和目标根目录。
- 不能包含删除动作。
- 移动文件必须有明确来源、目标和用户确认。
- 不能要求 CLI 生成知识正文。
- 不能默认提交到项目中心。

## `starworkKnowledge` skill 一期能力

一期只做项目本地知识库，不做项目中心提交。

### 能力 1：引导创建知识库

适用场景：

用户说“给这个项目开一个知识库”“我想让 AI 长期整理这个项目的知识”。

Agent 应做：

1. 判断当前工作台语言。
2. 向用户解释：知识库不是资料夹，是长期理解层。
3. 询问是否现在开启。
4. 如果是新建空知识库，调用 `starwork knowledge init`。
5. 如果涉及旧目录、定制路径或 AGENTS 规则调整，先生成 blueprint，再调用 `starwork knowledge apply`。
6. 根据项目情况微调 `schema.md` 和 `index.md`。
7. 不移动用户已有资料，除非用户明确确认。

### 能力 2：吸收资料

适用场景：

用户给了新资料，要求“整理到知识库里”。

Agent 应做：

1. 先读 `schema.md` 和 `index.md`。
2. 判断资料中是否有长期复用价值。
3. 更新或创建相关 `pages/`。
4. 必要时记录来源到 `sources/`。
5. 更新 `index.md`。
6. 更新 `log.md`。

### 能力 3：回答并沉淀

适用场景：

用户询问和项目长期知识相关的问题。

Agent 应做：

1. 先读 `index.md`。
2. 再读相关 `pages/` 和 `synthesis/`。
3. 回答用户问题。
4. 如果产生了可长期复用的新判断，经用户确认后更新知识库。

### 能力 4：形成综合判断

适用场景：

用户要求策略、复盘、阶段判断、方法论整理。

Agent 应做：

1. 读取相关 `pages/`。
2. 读取必要来源和历史 `synthesis/`。
3. 生成或更新一份 `synthesis/`。
4. 必要时把新概念反向沉淀到 `pages/`。
5. 更新 `index.md` 和 `log.md`。

### 能力 5：健康检查

适用场景：

用户说“检查一下知识库是否混乱”“整理一下知识库”。

Agent 应检查：

- `index.md` 是否能导航到主要页面。
- `pages/` 是否有重复主题。
- `synthesis/` 是否缺少引用的主题页。
- `inbox/` 是否长期堆积。
- `sources/` 是否能说明关键知识来源。
- 是否有空泛、无来源、无法复用的内容。

### 能力 6：生成知识库整理 Blueprint

适用场景：

用户已有旧 `知识/`、`knowledge/`、`资料库/` 或其他类似目录，希望整理成新知识库。

Agent 应做：

1. 读取 `starwork knowledge status --json` 或 `doctor --json`。
2. 判断旧目录更像什么角色。
3. 和用户确认是否开启新知识库。
4. 生成 `starwork.knowledge` blueprint。
5. 交给 CLI 执行结构性动作。
6. 再由 Skill 完成内容提炼和知识整理。

### 暂不做能力：提交到项目中心

旧备忘里提过 Promote，即把项目知识提交到项目中心共享知识库。

本轮暂不做。

原因：

- 项目知识库和项目中心共享知识的边界还需要单独设计。
- 审核、候选、合并、分发机制会显著增加复杂度。
- 当前优先级是先让单个项目里的知识库真正有用。

## CLI 配合调整

### `starwork init`

默认不创建知识库。

可以增加可选项：

```bash
starwork init --knowledge
```

交互式 init 中，知识库问题应靠后出现，且默认选“不需要”。

推荐文案：

```text
是否要为这个项目开启知识库？

知识库用于让 AI 长期整理项目里的稳定知识，不是放原始资料的文件夹。

> 暂时不需要（推荐）
  需要，帮我开启
```

如果用户选择开启，init 应复用 `starwork knowledge init` 的创建逻辑，而不是单独写一套结构。

### `starwork doctor`

`doctor` 不应把缺少 `知识库/` 或 `knowledge-base/` 当成问题。

它应该识别：

- 当前项目是否开启了知识库能力。
- 旧 `知识/` 或 `knowledge/` 更像资料夹、旧知识目录，还是项目中心链接。
- 如果存在 `知识库/`，检查基础结构是否完整。
- 如果存在多个类似知识目录，只暴露事实和风险，不直接建议合并。

### `starwork upgrade`

`upgrade` 不应自动把旧 `知识/` 迁移成新 `知识库/`。

合理策略：

1. 先由 `doctor` 暴露目录结构和内容信号。
2. 由 `starworkDoctor` 或后续知识库 skill 判断旧目录性质。
3. 如果用户确认要开启知识库，再生成升级蓝图。
4. `upgrade` 只按蓝图执行，不自行判断。

知识库相关升级动作应优先走：

```bash
starwork knowledge apply --blueprint <file>
```

而不是塞进通用 `upgrade` 的硬编码判断里。

### `starwork spawn`

项目中心创建项目工作台时，默认不强行挂载共享知识库为项目知识库。

后续如项目中心支持共享知识库，应另行设计：

- 项目本地知识库。
- 项目中心共享知识库。
- 二者之间是否只读、候选、审核和同步。

## Core 配合调整

后续实现时，建议新增：

```text
product/core/capabilities/knowledge/
├── capability.md
├── templates/zh/
└── templates/en/
```

该 capability 只描述项目本地知识库能力，不描述项目中心共享知识。

Project Kit 不应直接包含 `知识库/` 或 `knowledge-base/`。

`.starwork/workspace.json` 可增加能力声明：

```json
{
  "capabilities": {
    "knowledge": {
      "enabled": true,
      "root": "知识库",
      "mode": "local"
    }
  }
}
```

该声明只表示当前项目开启了本地知识库，不表示连接项目中心共享知识。

## AGENTS 规则配合

开启知识库能力后，应在项目 AGENTS 中增加简短规则：

中文示例：

```markdown
## 知识库

如果任务需要复用本项目的长期知识，先读 `知识库/index.md`。

不要把原始资料、临时草稿或单次任务过程直接塞进知识库。新资料需要整理成稳定主题页，放入 `知识库/pages/`；跨主题的策略、复盘和综合判断放入 `知识库/synthesis/`。
```

英文示例：

```markdown
## Knowledge Base

When a task depends on long-term project knowledge, read `knowledge-base/index.md` first.

Do not dump raw sources, temporary drafts, or one-off task notes into the knowledge base. Stable topics belong in `knowledge-base/pages/`; cross-topic strategy, reviews, and synthesized judgment belong in `knowledge-base/synthesis/`.
```

## 与 Pack 的关系

Pack 可以声明自己推荐开启知识库，但不应直接把知识库当作普通业务目录。

例如内容创作者 Pack 后续可以推荐：

```text
内容创作项目
+ General Pack
+ Knowledge Capability
```

但知识库仍由 Knowledge Capability 定义结构和维护规则。

## 与旧结构的关系

旧项目可能存在：

- `知识/`
- `knowledge/`
- `资料库/`
- `参考资料/知识/`
- Hub 软链接形式的 `knowledge/`

处理原则：

1. 不直接删除。
2. 不自动改名。
3. 不假设它就是新知识库。
4. 通过 `doctor --json` 暴露结构、文件数量、README 内容、是否软链接等信号。
5. 由 skill 结合用户确认生成整理建议。

## 实施阶段

### Phase 1：产品定义

- 输出本 SPEC。
- 和用户确认路径命名、目录结构、skill 一期能力。
- 确认是否从 Project Kit 移除默认 `知识/knowledge`。

### Phase 2：Core 能力模板

- 新增 `product/core/capabilities/knowledge/`。
- 提供中文和英文模板。
- 更新 Core / Kit 边界文档。

### Phase 3：Skill

- 新增 `starworkKnowledge` skill。
- 支持引导创建、吸收资料、回答并沉淀、综合判断、健康检查、生成知识库整理 blueprint。
- 暂不支持提交项目中心。

### Phase 4：CLI 命令组

- 新增 `starwork knowledge init`。
- 新增 `starwork knowledge status --json`。
- 新增 `starwork knowledge check`。
- 新增 `starwork knowledge apply --blueprint`。
- `init --knowledge` 复用 `starwork knowledge init`。

### Phase 5：CLI 识别和体检

- `doctor` 识别知识库能力是否开启。
- `doctor` 不再把缺少知识库当成问题。
- `upgrade` 只根据蓝图创建或整理知识库。

### Phase 6：文档

- 更新 README、CLI/skill registry、alpha guide。
- 给用户解释“知识库不是资料夹”。
- 给 Agent 写清楚如何使用知识库 skill。

## 验收标准

1. 新建普通项目时，不默认出现 `知识库/` 或 `knowledge-base/`。
2. 开启知识库能力后，生成标准结构。
3. `schema.md` 能明确告诉 Agent 如何维护知识库。
4. `pages/` 和 `synthesis/` 的区别在文档和 skill 中表达清楚。
5. `doctor` 不把缺少知识库当成结构错误。
6. 旧 `知识/knowledge` 不被自动迁移或删除。
7. skill 一期不出现“提交到项目中心”的默认动作。
8. 用户文案避免使用内部术语。
9. `starwork knowledge init --dry-run` 能展示将创建的结构。
10. `starwork knowledge status --json` 只暴露事实，不输出下一步建议。
11. `starworkKnowledge` 创建知识库时优先调用 CLI，不手写一套不一致结构。
12. 涉及旧目录整理时，必须先生成 blueprint 并让用户确认。

## 待用户确认

1. 中文路径是否确定为 `知识库/`。
2. 英文路径是否确定为 `knowledge-base/`。
3. 是否在下一轮正式移除 Project Kit 默认的 `知识/knowledge`。
4. `starworkKnowledge` 是否作为系统级 skill 安装，还是作为可选 skill 单独安装。
5. 内容创作者 Pack 是否未来默认推荐开启知识库能力。
6. `starwork knowledge` 命令组是否进入下一轮开发。
7. `init` 是否需要在交互流程里询问“是否开启知识库”，还是完全交给 skill 后续开启。
