# Project Structure

## 定位

本功能档案用于管理 StarWork 当前产品工作台自身的结构重整。

目标是将旧 `matters/`、根目录 `参考资料/`、根目录 `输出/` 的长期价值吸收到 `product/`，并建立按功能归档的产品规划结构。

## 当前状态

- 状态：执行中。
- 当前生效 SPEC：`specs/v0.1.md`。

## 相关范围

- 根 `AGENTS.md` 路由规则。
- `product/planning/` 规划区。
- `product/docs/` 文档索引。
- 旧 `matters/`、`参考资料/`、`输出/` 的移除和归档。

## 待定问题

- 旧 `matters/` 中哪些内容需要继续提炼进具体功能档案。
- `product/docs/` 中历史 M2.x SPEC 是否保留跳转文件。
- `roadmap.md` 的内部事实源是否迁到 `product/planning/roadmap/current.md`。
