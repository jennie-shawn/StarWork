# StarWork Core v0.1 构建

## 目标

构建 StarWork Core v0.1 的最小协议草案，并在确认后晋升到 `product/core/` 作为正式产品事实源。

## 当前会话

- Codex thread ID：`019e0b3f-120e-7b03-a5f4-c29c6b22ad1e`
- 创建时间：2026-05-09
- 事项状态：进行中

## 范围

- 明确 Core v0.1 的产品边界
- 定义 Agent 开始工作前必须读取的入口
- 定义身份、项目状态、决策、教训、当前工作、matter 的承载方式
- 定义 matter 创建、推进、归档的最小机制
- 定义默认只读文件与需要用户确认的动作
- 定义 Core 与 CLI、Packs、Adapters 的边界
- 产出可晋升到 `product/core/` 的协议草案

## 不在本事项范围

- CLI 具体实现
- 自媒体内容创作者 Pack 的专属目录和模板
- 旧 Runtime spike 的详细复核
- 课程与陪跑服务设计
- 账号、授权、消息平台 gateway 或复杂商业系统

## 事实源流转

- 过程讨论、拆解和临时草稿放在本事项。
- 可晋升内容先放入 `drafts/`。
- 定稿后写入 `product/core/`，晋升后以 `product/core/` 为准。
