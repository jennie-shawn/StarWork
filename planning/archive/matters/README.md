# Archived Matters

这里用于承接旧 `matters/` 的历史材料。

根目录 `matters/` 已移除，原始历史材料已整体归档到：

```text
legacy/
```

迁移规则：

- 已定稿结论提炼到对应功能的 `specs/` 或 `decisions.md`。
- 有价值讨论进入对应功能的 `discussions/`。
- 仅历史留痕的原始事项材料进入 `legacy/`。
